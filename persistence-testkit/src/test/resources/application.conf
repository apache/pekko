# SPDX-License-Identifier: Apache-2.0

pekko.persistence.testkit {
  events {
    assert-timeout = 500ms
    assert-poll-interval = 40millis
  }
  snapshots {
    assert-timeout = 500ms
    assert-poll-interval = 40millis
  }
}
